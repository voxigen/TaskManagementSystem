<Page x:Class="TaskManagementSystem.TasksView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Задания">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
           
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Задания" FontSize="20" FontWeight="SemiBold" Foreground="#2c3e50" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <RadioButton x:Name="AllTasksFilter" Content="Все" GroupName="TaskFilter" IsChecked="True" Checked="Filter_Changed"/>
                            <RadioButton x:Name="ActiveTasksFilter" Content="Активные" GroupName="TaskFilter" Margin="8,0,0,0" Checked="Filter_Changed"/>
                            <RadioButton x:Name="OverdueTasksFilter" Content="Просроченные" GroupName="TaskFilter" Margin="8,0,0,0" Checked="Filter_Changed"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <TextBlock Text="ФИЛЬТРЫ:" FontSize="12" FontWeight="SemiBold" Foreground="#7f8c8d" VerticalAlignment="Center"/>

                        <ComboBox x:Name="CourseFilter" Width="140" Height="28" SelectedIndex="0" Margin="12,0,0,0" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="Все курсы"/>
                            <ComboBoxItem Content="МДК 01.03"/>
                            <ComboBoxItem Content="ОП.05 БД"/>
                            <ComboBoxItem Content="Английский"/>
                        </ComboBox>

                        <ComboBox x:Name="StatusFilter" Width="120" Height="28" SelectedIndex="0" Margin="8,0,0,0" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="Все статусы"/>
                            <ComboBoxItem Content="На проверке"/>
                            <ComboBoxItem Content="Проверено"/>
                            <ComboBoxItem Content="Не сдано"/>
                        </ComboBox>

                        <ComboBox x:Name="SortFilter" Width="140" Height="28" SelectedIndex="0" Margin="8,0,0,0" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="По дате (новые)"/>
                            <ComboBoxItem Content="По дате (старые)"/>
                            <ComboBoxItem Content="По курсу"/>
                            <ComboBoxItem Content="По статусу"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Border>

     
            <StackPanel x:Name="TasksContainer">
              
            </StackPanel>

       
            <Border x:Name="NoTasksMessage" Style="{StaticResource CardStyle}" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                    <TextBlock Text="📝" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="Задания не найдены" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Нет заданий, соответствующих выбранным фильтрам" 
                              FontSize="12" Foreground="#7f8c8d" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>